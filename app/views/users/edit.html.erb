<div id="edit_user" class="prepend-6 append-6 span-12 last form-fat">
	<div class="round shadow mar10 settings">
		<p class="first-line round">
			Settings
		</p>
		<%= form_for(@user) do |f| %>
		  <%= render 'shared/error_messages', :object => f.object %>
		  <%= render 'fields', :f => f %>
		  <div class="actions">
		    <%= f.submit "Update", :class => 'button' %>
		  </div>
		<% end %>
		<!--<div>
		  <%= gravatar_for @user %>
		  <a href="http://gravatar.com/emails">change</a>
		</div>-->
	</div>
</div>
