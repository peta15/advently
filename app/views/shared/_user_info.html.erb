<div class="user_info">
  <a href="<%= user_path(current_user) %>">
		<%= fb_thumbnail_for(current_user) %>
    <span class="user_name">
      <%= current_user.name %>

    </span>
    <span class="microposts">
      <%= pluralize(current_user.tours.count, "tour") %>
    </span>

		<li><%= link_to "View all Users", users_path %></li>

  </a>
</div>